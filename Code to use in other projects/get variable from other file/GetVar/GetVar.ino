

void setup() {
    // this is just an example, so no setup code is needed.
}

void loop() {
  // put your main code here, to run repeatedly:
  /*
  
  We want to get the "foo" variable from the other script. how are we going to do this?
  well thats easy. just check what i do.
  
  */
  
  // first go to the other file.
  
  // welcome back to the first file :3
  
  // now lets grab the variable
  
  int bar = getfoo();
  
  // now the variable "bar" is the same as the "foo" variable of the first file :)
}
